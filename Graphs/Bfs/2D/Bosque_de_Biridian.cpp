/*
* Author: JorgeSLime     
* Problem: B. Bosque de Biridian
* Contest: Codeforces Ronda 192 (Div. 1)
* Judge: Codeforces
* URL: https://codeforces.com/problemset/problem/329/B
* Memory Limit: vim.NIL
* Time Limit: 2000
* Start: dom 16 jun 2024 21:23:36
* */
//THINK ONCE, CODE TWICE   "写之前要思考"
#include "bits/stdc++.h"
using namespace std;

#define sz(x)       (int)x.size()
#define all(x)    begin(x),end(x)
#define rall(x) rbegin(x),rend(x)
#define eb           emplace_back
#define LN                   '\n'
#define forn(i,n)  for(auto i=0;i<n;i++)
using i32=int32_t;
using i64=int64_t;

template<typename T> using V=vector<T>;

auto solve()->void{
    i32 n,m; cin>>n>>m; 
    V<V<char>> box(n,V<char>(m));
    for(i32 i=0;i<n;i++){
        
    }
}
auto main()->signed{
    cin.tie(nullptr)->sync_with_stdio(0);
    cin.exceptions(std::ios::failbit | std::ios::badbit);
    cout.tie(nullptr)->sync_with_stdio(0);
    i32 t=1; 
    //cin>>t;
    for(;t--;) solve();
    return 0;
}
